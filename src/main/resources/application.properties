# Server Port
server.port=8080

# Spring Application Name
spring.application.name=ams-backend

# MySQL Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ams_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=RO200053@jspch3
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Jackson Date Configuration
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.time-zone=UTC
